[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npm install -g pnpm@9.15.0",
  "pnpm install --frozen-lockfile"
]

[phases.build]
cmds = [
  "pnpm --filter @revanim/api run db:generate",
  "pnpm --filter @revanim/api run build"
]

[start]
cmd = "node apps/api/dist/index.js"
